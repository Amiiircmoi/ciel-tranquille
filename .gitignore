# --- Secrets (NE JAMAIS committer) ---
.env
.env.*
!.env.example
*.key
*.pem

# --- Environnements Python ---
.venv/
venv/
env/
ENV/
**/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Données générées (landing / curated) — régénérables ---
data/raw/
data/curated/
# Sorties annexes (captures, exports d'analyse) — défaut de CT_OUTPUT_DIR
outputs/
*.duckdb
*.duckdb.wal

# --- OS / IDE ---
.DS_Store
.idea/
.vscode/

# --- Fichiers locaux (non versionnés) ---
legacy/
*.pdf

# --- Artefacts régénérables (via make demo / make train) ---
# généré par ct-synth (les CSV réels restent versionnés)
data/samples/bruit_synth.csv
# modèle entraîné + rapports (régénérables)
models/*.joblib
models/*.json

notebooks/

# Données tierces brutes (Bruitparif/OpenSky) — local uniquement
data/real_survol/
data/real/
